// import React from "react";

const LogOut = () => {
  // faire un useState ou utiliser redux mais ne pas changer connected en dur
  // ça va se passer dans les cookies
  // ça devrait recharger la page en mode déconnecté automatiquement
};

export default LogOut;
