import React from "react";

// redux, si on obtient un reducer success, alors ça va redirect vers home
// connexion auto si c'est dans les cookies (cookies.get...)
const LogIn = () => <h1>Log in</h1>;

export default LogIn;
